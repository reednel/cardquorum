/** All supported game types in the platform. */
export type GameType = 'sheepshead';

/** Matches game_sessions.status values. */
export type GameSessionStatus = 'waiting' | 'active' | 'finished' | 'abandoned';
